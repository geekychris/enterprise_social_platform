{
  "name": "EntityPost",
  "log_type": "entity_post",
  "version": "1.0.0",
  "description": "CDC events for post entity changes",
  "kafka": {
    "topic": "worksphere-entity-posts",
    "partitions": 4,
    "replication_factor": 1,
    "retention_ms": 2592000000
  },
  "warehouse": {
    "table_name": "warehouse.posts_hourly",
    "partition_by": ["event_hour"],
    "sort_by": ["event_timestamp", "id"],
    "retention_days": 90
  },
  "fields": [
    {"name": "event_type", "type": "string", "required": true, "description": "CREATE, UPDATE, DELETE"},
    {"name": "event_timestamp", "type": "timestamp", "required": true, "description": "When the change occurred"},
    {"name": "event_hour", "type": "string", "required": true, "description": "Partition key YYYY-MM-DD-HH"},
    {"name": "event_date", "type": "date", "required": true, "description": "Event date"},
    {"name": "id", "type": "long", "required": true, "description": "Post ID"},
    {"name": "author_id", "type": "long", "required": true, "description": "Author user ID"},
    {"name": "content", "type": "string", "required": false, "description": "Post content"},
    {"name": "visibility", "type": "string", "required": false, "description": "PUBLIC, PRIVATE, etc"},
    {"name": "target_type", "type": "string", "required": false, "description": "Target entity type"},
    {"name": "target_id", "type": "long", "required": false, "description": "Target entity ID"},
    {"name": "created_at", "type": "string", "required": false, "description": "Entity creation time"}
  ]
}
